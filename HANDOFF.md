# HANDOFF — CP_RAG context layer (updated 2026-09-08)

## Done this session
- **Corpus provenance audit (0 credits)** — grepped all 3,785 indexed chunks (`chroma_db/chroma.sqlite3`, `embedding_fulltext_search_content` + per-chunk `source` metadata) to verify every linkage claim. Result: humidity→rhinitis named mites/moulds (corpus); heat/dengue partially supported (dehydration + SGLT2 sick-day rules in corpus, triggers not); pm25/psi/rain not in any protocol. Evidence table: `docs/signal-research.md` §12.
- **Option A provenance tagging implemented** — every rule in `context/linkage.py` carries `basis` (corpus/partial/derived/none) + `basis_note`; `build_protocol_links` surfaces both on each active link; `snapshot.py` adds `basis_legend` + two-section Phase-2 usage note; F2 strip (`static/index.html`) renders a badge per link (JS whitelist, unknown → `derived`), audit note on hover, legend. Dengue rule over-claim fixed: "hold metformin/SGLT2…" → "stop SGLT2 inhibitors while acutely unwell (CKD + multimorbidity protocols)" (corpus-exact; §11 example table fixed too).
- **Verified live** — restarted on :5001, `{"ready":true}`, cold `GET /api/context` 200 in ~13 s: 4 active links with correct basis values (pm25 derived, heat partial, rain derived, dengue partial), `data_gaps: []`, no errors in log.
- **Committed** — `b06ddad` (env-configurable provider) + `266b73a` (context layer: app.py, static/index.html, context/, docs/, CLAUDE.md, HANDOFF.md; 13 files, +1863). Working tree clean. Local main is **2 commits ahead of origin** (unpushed).
- **MOH haze page assessed** — `moh.gov.sg/others/haze/` is static public guidance (no live data; links out to NEA, which we already fetch): PSI-band × population-group health advisory table, N95/hydration/medication advice. Ingestion into the corpus **saved for Phase 2** (see Outstanding #1b).

## Outstanding
1. **Phase 2 (next)** — F3 RAG integration + corpus expansion, in `rag.py`:
   - **1a. Context injection** — in `build_chain`, inject active links into the prompt as two labelled sections: *Protocol content* (only corpus/partial material, per each link's `basis_note`) and *Local context* (all live signals, never attributable to the protocols); `derived` links must never be presented as protocol content. Also append active protocol names to the retrieval query (mechanism B). Design choice: how the chain gets the snapshot — startup build / per-request via the same 15-min cache as `/api/context` (cold build 15–40 s, so never build inside the chat request path without caching) / fixed clinic point via env.
   - **1b. MOH haze page → corpus** (user-deferred into Phase 2) — multi-source `rag.py`: `SOURCE_URL` → list of (name, url, max_depth); `moh.gov.sg/others/haze/` at **depth 0** (moh.gov.sg is huge); append-only ingest mode that embeds only the new docs (~1–2 chunks, pennies) and never re-embeds the existing 3,785; add `source_site` metadata; content-node-targeted extractor (Isomer boilerplate: gov.sg banner/nav/footer); provenance label *public guidance* (third prompt section — not protocol content); then update `pm25`/`psi` `basis_note`s to cite it (counseling text becomes citable government text).
2. Phase 3 — WIDB PDF table parser (pdfplumber/pypdf) → `disease_week` block.
3. Phase 4 (optional) — URA Planning_Decision catchment block (`URA_ACCESS_KEY`; key in `/tmp/ura_key.txt`).
4. `:free` model caveats — OpenRouter free tier has lower rate limits (429 on bursts) and the model can be swapped; switch `CHAT_MODEL` to `openai/gpt-4o` in `.env` if needed (paid).
5. Postcode geocoding is district-centroid approx (OneMap DNS-blocked on this network); `--lat/--lon` is the exact path.
6. `app.py` has no `uvicorn.run()` — `python app.py` exits silently; always use `venv/bin/uvicorn app:app --port 5001`.
7. Local main is 2 commits ahead of origin — `git push` when desired.

## Next-session prompt
Project: CP_RAG at `/Users/ugeneo/Documents/Project Codes/CP_RAG` — FastAPI + LangChain RAG Q&A over Singapore primary-care chronic-care protocols (corpus: primarycarepages.sg /Healthier-SG/Care-Protocols/Chronic, 17 protocols, 3,785 chunks, Chroma at `./chroma_db`). LLM: `nvidia/nemotron-3.5-lightning:free` (free chat) + `text-embedding-ada-002` embeddings, both via **OpenRouter** (OpenAI-compatible; provider/models env-configured in `.env` — OpenRouter key, git-ignored, never commit it; see `rag.py` header). Golden rules in `CLAUDE.md`: OpenAI credits are a budget (no chat probes except the 1–2 Phase 2 verification chats; never re-ingest "just to test"), Python 3.9, `chroma_db/` is expensive — don't rebuild. On top of the RAG app, a `context/` package (stdlib-only, key-free data.gov.sg) builds a live per-clinic snapshot (NEA PM2.5/PSI, weather/WBGT/flood, dengue clusters, aedes areas, polyclinics) with signal→protocol linkage; `GET /api/context` serves it (15-min in-memory cache, `X-Snapshot-Cache` header) and the F2 strip at the top of `static/index.html` renders it. Every active link is provenance-tagged: `basis` (corpus/partial/derived/none) + `basis_note` from the 2026-09-08 corpus audit (`docs/signal-research.md` §12) — rendered as badges in the UI; **`derived` links must never be presented as protocol content**. Read `CLAUDE.md` and `docs/signal-research.md` (§9 phase table, §11 Phase 0 build notes, §12 provenance audit) before starting.

State: Phases 0 and 1 are DONE, verified live, and committed (`b06ddad` provider, `266b73a` context layer; tree clean; local main 2 ahead of origin). Test clinic: Woodlands Polycline (lat 1.4309, lon 103.7752, PC 738579) — last run: 4 active links (PM2.5, WBGT heat, rain Wed–Fri, dengue cluster 2.7 km) with basis tags derived/partial/derived/partial, `data_gaps: []`, cold build ~13 s. Chat verified end-to-end on the free model.

Task — Phase 2 (per `docs/signal-research.md` §9): (A) In `rag.py build_chain`, inject the active protocol links into the prompt as two labelled sections — *Protocol content* (only corpus/partial material, per `basis_note`) and *Local context* (all live signals, never attributable to the protocols) — and append active protocol names to the retrieval query (no re-embedding). Design choice: how the chain gets the snapshot (startup / per-request via the 15-min cache / fixed clinic point via env). (B) Add the MOH haze page (`moh.gov.sg/others/haze/`, static public guidance — PSI-band × population-group advisory table) to the corpus: multi-source `rag.py` (moh at depth 0), append-only ingest (embed only new chunks), `source_site` metadata, Isomer-aware extractor, provenance label *public guidance* (third section), then update `pm25`/`psi` `basis_note`s to cite it. Verify with 1–2 targeted chats (free tier, low rate limits — keep minimal) and update the §9 phase table when done.

