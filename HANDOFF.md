# HANDOFF — CP_RAG (updated 2026-09-08, Phase 3 — CDA WIDB weekly infectious-disease bulletin)

## Done this session (Phase 3 — WIDB, changes verified — user commits per CLAUDE.md)
CDA Weekly Infectious Disease Bulletin (PDF-only, weekly) is now a population-level signal in the context snapshot, the RAG prompt, the CLI, and the UI. 0 API credits (key-free gov.sg sources).
- **`context/config.py`** — `WIDB_ARCHIVE_URL` (per-year CDA archive page), `WIDB_UA` (browser User-Agent — the PDFs are on S3 which 403s the default python UA), `WIDB_MAX_LOOKBACK = 5`, `WIDB_CACHE_TTL_SECONDS = 3 days`.
- **`context/http.py`** — `get_bytes()` accepts optional `extra_headers`.
- **`context/widb.py`** (new) — `list_bulletins(year)` crawls the 2026→2025 archive pages for `EW NN` PDF links; `fetch_latest()` walks newest→oldest, downloads (percent-encoded URLs), checks the `%PDF` magic (S3 AccessDenied error pages are rejected, not parsed), parses, disk-caches `widb_latest.json`. A broken newest file (observed live: EW 34 403s from S3 intermittently) falls back to EW 33. `parse_widb()` (pypdf — already a Phase-5 dependency, nothing new added): page-1 disease table (`week/prev_week/median_5yr` + `cum/cum_prev` for notifiables; column mapping verified arithmetically, EW32 cum + EW33 week == EW33 cum for dengue) + flattened-page narrative regexes (dengue notifications/admissions/serotypes, influenza type mix + ILI positivity, COVID-19 ARI positivity, top ARI pathogens, ARI polyclinic attendances).
- **Parser bugs found + fixed during verification:** (1) row regex allowed digits in disease names → backtracking across multi-digit cells dropped rows (name class now excludes digits, 1+ space separator); (2) "…in E-\nweek 33" wraps across a line break → ARI-attendances regex allows `E-\s?week`; (3) name-only line with numbers on the next line (e.g. `Mpox#`) → two-line fallback. Verified against EW 33, EW 32 and EW 1 by hand.
- **Wiring** — `snapshot.py` adds `disease_week` (a `widb: <reason>` entry goes to `data_gaps` on failure, never crashes the snapshot); `prompts.py` adds one *Local context* line ("WIDB 2026-W34 … national counts: …", labelled not protocol content); `__main__.py` prints a `DISEASE` section and `--no-cache` now also refetches the bulletin; `static/index.html` adds a WIDB chip to the strip + an "Infectious diseases (WIDB, national)" detail card (YTD, admissions, notifications, flu, ARI, bulletin-PDF link).
- **Verified (0 credits):** live `fetch_latest()` → EW34 (23–29 Aug 2026), 3-day cache hit on second call; walk-back + total-failure paths exercised deterministically with a monkeypatched `get_bytes` (EW34 S3-error → falls back to EW33; all-fail → `(None, error)`); `python -m context --no-cache` full snapshot; `/api/status` → `ready:true`; `/api/context` → `disease_week` present, `data_gaps: []`; `node --check` on the page script OK; server restarted and serving.
- **Docs:** `docs/signal-research.md` §9 (Phase 3 DONE) + §14 build notes; `CLAUDE.md` context-layer bullet + repo layout; local ledger `.superpowers/sdd/2026-09-08-widb-bulletin/ledger.md` (gitignored, like the ACE one).

## Outstanding (priority order)
1. **Commit Phase 3** — 9 modified files + untracked `context/widb.py` (nothing staged). Suggested: `feat: Phase 3 — CDA WIDB weekly bulletin as disease_week context signal`.
2. WIDB is layout-fragile by design (regex over PDF text). If CDA changes the bulletin template, `parse_widb()` fails loud (`dengue row missing` / `no EPIDEMIOLOGICAL WEEK header`) and the snapshot shows a `widb:` gap; the 3-day cache can serve a stale parse meanwhile — `python -m context --no-cache` force-refetches. Recover by re-checking a fresh PDF dump against the new layout.
3. Carried from the Phase 5 post-completion review (pre-existing patterns, none security-relevant):
   - `app.py` `/api/ingest-pdf`: the 25 MB cap is checked only after `await file.read()` has already buffered the full body.
   - `rag.py` `ingest_pdf()` / `ingest_append()`: hash/URL dedupe is check-then-act with no lock (concurrent duplicate uploads can double-embed); `_store_pdf_index()` duplicates `_store_url_index()`'s scan loop.
   - `rag.py` `list_pdfs()` / `_store_pdf_index()`: full metadata scan of the whole Chroma store on every call, O(n), no caching.
4. 1 scanned ACE PDF uningested (rehab appendix) — OCR would be needed; revisit only if that appendix matters.
5. ACE repo drift: `venv/bin/python scripts/ace_guidelines.py --list` (free) shows it; `--ingest` re-runs are idempotent (sha256 dedupe).
6. k=12 retrieval heuristic — revisit if answers over- or under-retrieve as the corpus grows.
7. primarycarepages refresh needs a full `ingest()` rebuild (URL rewrite defeats append skip); postcode geocoding is district-centroid approx (OneMap DNS-blocked) — `--lat/--lon` is exact.

## Next-session prompt
Project: CP_RAG at `/Users/ugeneo/Documents/Project Codes/CP_RAG` — FastAPI + LangChain RAG Q&A over Singapore primary-care chronic-care protocols **plus the full ACE clinical-guidelines repository (96 ACG PDFs, 2,975 chunks; `source_site` routes them to the *Clinical guidelines* prompt section — never cited as protocol content)** + MOH public guidance + live local-context signals **now including the CDA weekly infectious-disease bulletin (`disease_week`: national dengue/HFMD/influenza/ARI/COVID counts, Phase 3)**. LLM `deepseek/deepseek-v4-flash-0731` + `text-embedding-ada-002` via OpenRouter (env-configured in `.env`, git-ignored — never commit it). Golden rules in `CLAUDE.md`: credits are a budget (prefer `/api/status` over chat probes), Python 3.9, `chroma_db/` is expensive — don't rebuild; new guideline PDFs go through `POST /api/ingest-pdf` (append-only, sha256 dedupe) or `scripts/ace_guidelines.py --ingest`. Corpus audit + phase table + WIDB build notes: `docs/signal-research.md` §9/§12–14. Read `CLAUDE.md` first.

State: Phases 0–3 + 5 DONE and verified live; Phase 3 changes are on disk but **uncommitted** (9 modified files + `context/widb.py`, nothing staged — HEAD is still `e329055`, the Phase 5 XSS fix). Prior phases' SDD ledgers in `.superpowers/sdd/` (gitignored, local-only). Next candidate per the phase table: **Phase 4 (URA planning-decision catchment — needs `URA_ACCESS_KEY` in `.env`; the API was verified working 2026-09-08, docs §10)**. Server running detached on port 5001 (log: `/tmp/cp_rag_server.log`). Test clinic: Woodlands Polycline (lat 1.4309, lon 103.7752, PC 738579). Corpus: protocols + MOH + 96 ACE guideline PDFs (~2,975 guideline chunks).
