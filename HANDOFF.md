# HANDOFF — CP_RAG (updated 2026-09-08, Phase 4 — URA planning-decision catchment signal)

## Done this session (Phase 4 — URA, all gates green, committed)
URA e-Services `Planning_Decision` (written permissions, daily) is now a live population-level signal in the context snapshot, the RAG prompt, the CLI, and the UI — following the Phase 3 (WIDB) pattern exactly. 0 API credits (no embedding/LLM calls; stdlib-only `context/`).
- **`context/config.py`** — `URA_BASE`, `URA_WINDOW_DAYS = 90`, `URA_CACHE_TTL_SECONDS = 24 h`, `URA_MAX_ITEMS = 20`, `URA_HEALTHCARE_KEYWORDS` (POLYCLINIC, CLINIC, MEDICAL, NURSING HOME, CHILD CARE, SENIOR), `URA_ACCESS_KEY` (machine-parsed from `.env` with env fallback, like `DGS_API_KEY`).
- **`context/ura.py`** (new) — `get_token()` (daily token via `insertNewToken/v1`, `AccessKey` header); `fetch_rows()` (`invokeUraDS/v1?service=Planning_Decision&last_dnload_date=<today−90d dd/mm/yyyy>`, `AccessKey` + `Token` headers; row fields per official docs: dr_id, submission_no, decision_no, decision_date, decision_type, submission_desc, address, mkts_lotno, delete_ind); `filter_healthcare()` (keyword match on `submission_desc`, `delete_ind == 'Yes'` dropped, newest `decision_date` first, dd/mm/yyyy → ISO); `fetch_catchment()` → `(payload, error)`, 24 h disk cache (`$TMPDIR/cp_rag_context_cache/ura_planning.json`), never raises. No key → `ura: URA_ACCESS_KEY not set in .env (Phase 4 signal disabled)` in `data_gaps`.
- **Wiring** — `snapshot.py` adds `catchment_change` (source, window, rows_scanned, `healthcare_decisions_90d_count`, latest-20 list of `address/what/date/decision_type/decision_no`, `caveat`); `prompts.py` adds one *Local context* line (up to 3 examples, explicitly "NOT protocol content" and "a written permission is NOT an opened facility"); `__main__.py` prints a `PLANNING` section, `--no-cache` now also refetches the URA window; `static/index.html` adds a `URA 90d` chip + "Planning decisions (URA, island-wide)" card with the caveat line.
- **Scope ruling (vs the §10 sketch):** `healthcare_approvals_90d` → `healthcare_decisions_90d` (the service returns multiple `decision_type` values; each row shows its own — observed live: `Written Permission`/`Authorized Work`/`Corrigendum`, no refusals in the 2026-09-08 window); `new_resi_units_approved_region` dropped (no units field in the service; rows have street addresses only, so regional aggregation needs OneMap geocoding — deferred, recorded in `docs/signal-research.md` §10 ruling + §15). The block is island-wide and labelled as such everywhere.
- **Verified:** monkeypatched transport gates (`/tmp/verify_ura_task1.py`, `/tmp/verify_ura_task2.py` — 0 network, 0 credits): filter/sort/delete-skip, cache-hit no-refetch, no-key gap, token-HTTP-failure, bad-Status; prompt line renders with caveat, absent block adds nothing; no-key `python -m context` → gap + no `PLANNING` section; seeded-cache `python -m context` → `PLANNING` section (newest first); server restarted, `/api/status` → `ready:true`, `/` → 200, `node --check` on the page script OK.
- **Live-verified (2026-09-08, plan Appendix A):** `URA_ACCESS_KEY` added to `.env` (git-ignored); fresh fetch → window 2026-06-10→2026-09-08, **2,363 rows scanned, 74 healthcare-related decisions** (keyword overlap CLINIC 45 / MEDICAL 36 / CHILD CARE 13 / SENIOR 8 / POLYCLINIC 4 / NURSING HOME 6); **`decision_type` across all 74: `Written Permission` 67, `Authorized Work` 6, `Corrigendum` 1** — no refusal-type rows (code only displays the type, never branches); a few rows carry `decision_date`s earlier than the window start (API filters by record created/modified date, not decision date). `python -m context` → real `PLANNING` section (20 rows, newest first, e.g. 2026-08-27 additions & alterations to Bukit Batok Polyclinic); server restarted with the key → `/api/context` `catchment_change` present, `data_gaps: []`; negative check (key commented out) → `ura:` gap + no `PLANNING`, key restored; `/` → 200 with the card markup. Full numbers recorded in `docs/signal-research.md` §15 + the ledger.
- **Docs:** `docs/signal-research.md` §9 (Phase 4 DONE) + §10 v1 scope ruling + §15 build notes; `CLAUDE.md` context-layer bullet + repo layout; plan at `docs/superpowers/plans/2026-09-08-ura-planning-decisions.md`; local SDD ledger `.superpowers/sdd/2026-09-08-ura-planning/ledger.md` (untracked, local-only, like the others).
- **Commits (on `main`, ahead of `origin/main`):** `7069a50` ura.py + config; `2645940` snapshot/prompt/CLI; `4638e44` UI chip + card; `838e7df` docs (signal-research §9/§10/§15, CLAUDE.md, HANDOFF, plan); + the live-verification docs commit.

## Outstanding (priority order)
1. Catchment geocoding for per-clinic planning signals: OneMap Geocoding API (free but needs its own key) or a street→region table — rows carry street addresses only, no postcode/region.
2. WIDB is layout-fragile by design (regex over PDF text). If CDA changes the bulletin template, `parse_widb()` fails loud (`dengue row missing` / `no EPIDEMIOLOGICAL WEEK header`) and the snapshot shows a `widb:` gap; the 3-day cache can serve a stale parse meanwhile — `python -m context --no-cache` force-refetches.
3. Carried from the Phase 5 post-completion review (pre-existing patterns, none security-relevant):
   - `app.py` `/api/ingest-pdf`: the 25 MB cap is checked only after `await file.read()` has already buffered the full body.
   - `rag.py` `ingest_pdf()` / `ingest_append()`: hash/URL dedupe is check-then-act with no lock (concurrent duplicate uploads can double-embed); `_store_pdf_index()` duplicates `_store_url_index()`'s scan loop.
   - `rag.py` `list_pdfs()` / `_store_pdf_index()`: full metadata scan of the whole Chroma store on every call, O(n), no caching.
4. 1 scanned ACE PDF uningested (rehab appendix) — OCR would be needed; revisit only if that appendix matters.
5. ACE repo drift: `venv/bin/python scripts/ace_guidelines.py --list` (free) shows it; `--ingest` re-runs are idempotent (sha256 dedupe).
6. k=12 retrieval heuristic — revisit if answers over- or under-retrieve as the corpus grows.
7. primarycarepages refresh needs a full `ingest()` rebuild (URL rewrite defeats append skip); postcode geocoding is district-centroid approx (OneMap DNS-blocked) — `--lat/--lon` is exact.

## Next-session prompt
Project: CP_RAG at `/Users/ugeneo/Documents/Project Codes/CP_RAG` — FastAPI + LangChain RAG Q&A over Singapore primary-care chronic-care protocols **plus the full ACE clinical-guidelines repository (96 ACG PDFs, 2,975 chunks; `source_site` routes them to the *Clinical guidelines* prompt section — never cited as protocol content)** + MOH public guidance + live local-context signals **now including the CDA weekly infectious-disease bulletin (`disease_week`, Phase 3) and URA planning decisions (`catchment_change`, Phase 4 — island-wide healthcare-related planning decisions, 90-day window; a written permission is NOT an opened facility; **live-verified 2026-09-08**, key in .env)**. LLM `deepseek/deepseek-v4-flash-0731` + `text-embedding-ada-002` via OpenRouter (env-configured in `.env`, git-ignored — never commit it). Golden rules in `CLAUDE.md`: credits are a budget (prefer `/api/status` over chat probes), Python 3.9, `chroma_db/` is expensive — don't rebuild; new guideline PDFs go through `POST /api/ingest-pdf` (append-only, sha256 dedupe) or `scripts/ace_guidelines.py --ingest`. Corpus audit + phase table + build notes: `docs/signal-research.md` §9/§12–15. Read `CLAUDE.md` first.

State: **Phases 0–5 ALL DONE**, including Phase 4 **live-verified 2026-09-08** (2,363 rows / 74 healthcare-related in the 90-day window; `decision_type` values observed: `Written Permission` 67, `Authorized Work` 6, `Corrigendum` 1 — §15), and committed on `main` (5 Phase-4 commits ahead of `origin/main` `bcfcc44` — push when you commit). SDD ledgers in `.superpowers/sdd/` (untracked, local-only). Server running detached on port 5001 (log: `/tmp/cp_rag_server.log`; restarted this session to load the Phase-4 code). Test clinic: Woodlands Polycline (lat 1.4309, lon 103.7752, PC 738579). Corpus: protocols + MOH + 96 ACE guideline PDFs (~2,975 guideline chunks). No further phases remain in the §9 table — remaining work is the Outstanding list above.
