# HANDOFF — CP_RAG (updated 2026-09-09 — test clinic swap after Phase 6)

## Done this session (2026-09-09, post-Phase 6 — test clinic swap: polycline → GP clinic)
Test clinic changed from Woodlands Polycline to **Lakeside Family Medicine Clinic**, 518A Jurong West Street 52, PC 641518 (1.3454017, 103.7188383 — real **GP-clinic** point, OSM node 8083778017, verified live 2026-09-09; same-day interim swaps to Pioneer Polyclinic and DA Clinic @ Taman Jurong were superseded — first at user request, a polycline is not the app's GP persona, then by user choice between the two GP finalists). Final verified state (post-restart `/api/context`, 0 LLM credits): west PM2.5 52 (island peak 85 central — haze building) **and** a **60-case dengue cluster** (Ho Ching Rd, upd 2026-09-03) **1.1 km** away + a 2-case cluster **0.3 km** away → 4 active protocol links (pm25/rain/dengue_cluster/humidity; heat dropped as WBGT eased), `data_gaps: []`, nearest polyclinic tile = Pioneer 2.3 km. Method: ranked all 8 polyclines in the vaccination layer, then searched OSM (Nominatim, key-free) for `amenity=clinic` POIs in the west region and A/B-snapshotted the two finalists (DA Clinic vs Lakeside — user picked Lakeside). Also clarified the URA tile for the user (74 = healthcare-related URA planning decisions/written permissions in the trailing 90 days; approval ≠ facility opening) — no code change. Changed: `context/config.py` TEST_CLINIC, `context/__init__.py` docstring example, `docs/signal-research.md` historical note, `HANDOFF.md` — **all 4 uncommitted (user signals when to commit)**. Server restarted on port 5001 (brief cache cold after restart — by design; no brief generated for the new clinic yet, Generate = 1 paid call).

### Phase 6 + debt fixes (2026-09-09, committed + pushed — historical detail, all gates green)
The chat page is now three hash-routed tabs — Brief (default) / Chat / Library. The Brief tab is a clinician dashboard: 6 KPI tiles from the live snapshot + an on-demand one-page brief (headline / watch list / outlook) + a Protocol spotlight rendered 100% from structured `protocol_links.active`. The Library tab lists the corpus (totals, guideline-PDF grid, protocol pages, MOH guidance) and now hosts the PDF upload bar. **Phase credit total: 2 paid LLM calls** (budget was 1 — recorded deviation below); every other path verified at 0 credits.
- **`context/brief.py`** (new) — pure, stdlib-only snapshot→prompt projection; `protocol_links` structurally absent from the LLM payload (asserted in dry runs); strict JSON contract `{headline, watch[≤6], outlook}`.
- **`rag.py`** — `list_corpus()` (Task 1: PDFs + web sources by site + totals; legacy chunks without `source_site` fall back to URL host via `_site_of()`); `generate_brief()` (one chat-model call, no retrieval, no embeddings), `_parse_brief()` (fence-strip, strict JSON, type coercion, watch clip 9→6), `_apply_provenance_guard()` (drops watch items mentioning `protocol`/`guideline`/known protocol names; counted as `provenance_drops`).
- **`app.py`** — `GET /api/library` (503 on `rag_chain is None`, executor scan, 500 on scan failure); brief cache state (in-memory, keyed on 3-dp clinic coords, tied to the snapshot's `generated_at`; server restart = cold); `GET /api/brief` (free, never builds); `POST /api/brief/generate` — **the only LLM path outside `/api/chat`**, guard order per spec §7.2: invalid point 400 → KB-not-ready 503 → fresh cache (0 credits) → cached-error 502 (90-s failure sentinel) → in-flight shared future (stampede guard) → forced-regenerate 429 + `Retry-After` (60-s cooldown) → thread-pool build; 15-min success TTL.
- **`static/index.html`** — `<nav id="tabs">` + hash routing (`#/brief` default, `#/chat`, `#/library`; bogus hash → brief); 6 KPI tiles (Air/Weather/Dengue/WIDB/Planning/Nearest polyclinic, gap + degraded states per D10); brief renderer (headline / watch cards / outlook / provenance footer + parse-error fallback showing raw LLM text + "No brief yet" empty state); `generateBrief()` (the UI's only brief POST; double-click guard; staged progress; button flips to "Regenerate (costs 1 LLM call)" only when a fresh brief exists); `loadLibrary()`/`renderLibrary()` (totals line, 96-card PDF grid with source links + doc-hash prefixes, protocol section filtered to `/care-protocols/` with the >25 URL-sprawl collapse card + "49 other pages" footnote, MOH section with derived title + raw URL); `#pdf-bar` moved from Chat to Library; context strip survives verbatim in the Brief view, collapsed under "Raw context data" (DOM nodes never removed — the 15-min refresh intervals depend on them).
- **Scope rulings (spec, one-liners):** D1 full Library (PDFs + protocol pages + MOH + totals, not PDFs only); D2 Brief is the home view, the 7-card strip survives verbatim as a collapsed drill-down; D3 on-demand LLM (no retrieval/embeddings, 15-min cache per clinic point); D4 two-phase API (free GET + paid POST); D5 in-flight stampede guard (concurrent POSTs await one future → 0 extra LLM calls); D6 server-side 60-s force-regenerate cooldown → 429 + `Retry-After`; D7 90-s failure sentinel cached on LLM failure → 502, outage retries cost 0; D8 "1 Generate" = ≤2 provider round-trips (~2–3k tokens, 0 embeddings, 0 retrieval) — stated in UI microcopy + docs; D9 clinical-safety provenance (prompt strip + structured-only spotlight + server-side drop guard); D10 per-tile "unavailable (source)" gap states keyed off `data_gaps`; D11 task order Library backend → brief backend → UI → docs.
- **Verified (gates, all against the live server):** server boot + `/api/status` ready + `/` 200; `node --check` on the extracted page script; headless DOM harnesses with **zero runtime errors** — Brief harness (6 tiles, headline, 6 watch cards, outlook, spotlight, tab round trip incl. a manual `refreshContext()` tick from the Chat tab, bogus hash → brief) and Library harness (direct reload at `#/library`, 13/13: totals vs API, 96 PDF cards + collapse card, protocol "28 pages — collapsed" + "49 other pages" footnote, MOH row with raw URL + derived title, round trip). Library API 0 credits: totals **6,764 chunks / 96 guideline PDFs (2,975 chunks, matches Phase 5) / 28 protocol pages / 1 public-guidance page**. Free brief endpoints: stale/none + 400 bad point. **Paid record:** `POST /api/brief/generate` → 200, `deepseek/deepseek-v4-flash-0731`, valid JSON, headline + 5 sourced watch items, `provenance_drops: 0`, outlook names the PM2.5 gap (~91 s wall = cold snapshot + LLM); forced regenerate → **429 + `Retry-After: 59`** (0 credits); no-force after fresh → `cached: true` (0 credits, curl-verified).
- **Credit deviation (recorded in the ledger):** budget was 1 paid call; **2 were consumed.** The boot-time context warm-build completed mid-verification, rotating the 15-min snapshot cache; a no-force POST issued as a "cache probe" (status probes should use the FREE `GET /api/brief`) then saw the rotated snapshot → correct stale detection → paid rebuild. Cache/invalidation worked as designed; the probe was the deviation. No further paid calls this phase.
- **Commits (on `main`, ahead of `origin/main`):** `7454cce` spec + plan; `6403ff3` Task 1 (`/api/library`); `831a364` Task 2 (brief backend); `af1e31d` Task 3 (tabs + Brief UI); `22ed7ec` Task 4 (Library UI); + this docs commit.

- **Post-completion debt fixes (2026-09-09, 0 credits):** (1) `app.py` `/api/ingest-pdf` now streams the upload in 1 MB chunks and refuses at the 25 MB cap — a 500 MB test upload returned 400 with the server's RSS up only ~27 MB (previously the whole body was buffered before the cap check); 26 MB → 400, non-PDF → 400 (regression), no embedding activity in the log. (2) `rag.py`: the O(n) full-metadata scan is now cached in `_scan_metadatas()` (invalidated on `vectorstore._collection.count()` — O(1) in chromadb; note the langchain 0.2.x `Chroma` wrapper has no `count()` of its own), shared by `list_pdfs()` / `list_corpus()` / `_store_url_index()` / `_store_pdf_index()` — direct measure: 173.5 ms scan vs 12.5 ms cache hit, `list_corpus` ~19 ms; HTTP `/api/library` cold 183 ms / warm 21 ms (was ~100–280 ms per call); data-equality asserted across cache/invalidation states; both headless harnesses re-run, zero runtime errors.
## Outstanding (priority order)
1. Commit the test clinic swap — 4 files modified/unstaged (`context/config.py`, `context/__init__.py`, `docs/signal-research.md`, `HANDOFF.md`); user signals when ready.
2. No brief generated for the new clinic — brief + snapshot caches are in-memory and cold after restart (by design); Generate in the UI costs 1 chat-model call (user's call), `GET /api/brief` is the free status probe.
3. Phase-6 paid-call deviation (2 vs 1 budgeted) — recorded in SDD ledger §16 + `docs/signal-research.md`; fold into the next docs commit.
4. Library upload has no progress indicator (UI): the 12 s for the 25 MB cap is a blind wait; add % or a spinner.
5. 4.8 s upload→index sync gap (UX): brief/library see a file ~5 s after upload; acceptable for now.
6. URA cache TTL 24 h (dev ergonomics): stale planning decisions possible in a demo if URA publishes intraday; shorten or add a refresh affordance.
7. `d_b20e71d9` (vaccination clinics) has no "Updated" field — cannot verify live-refresh; treat its "as-of" as unverified.
8. data.gov.sg is the sole external data source — no retry/circuit breaker on fetch (reliability debt); one 5xx degrades the snapshot until the 24 h TTL expires.
9. Cold start 6.14 min (reliability debt): ~4.6 min of it is embedding 2,716 ACE guideline chunks; precompute/serve embeddings out of process, or lazy-index.
10. `context/` has no tests (optional stretch): snapshot assembly, threshold logic, fetcher parsing are the risky pieces.
11. 2,716-chunk ACE corpus indexed from a single PDF — verify content completeness (quality debt, optional).

Lower-priority carry-overs (unchanged, none acted on this session): live PDF upload unexercised this phase (`POST /api/ingest-pdf` verified in Phase 5, unchanged); primarycarepages refresh needs a full `ingest()` rebuild (URL rewrite defeats append skip); postcode geocoding is district-centroid approx (OneMap DNS-blocked) — `--lat/--lon` is exact; `rag.py` ingest dedupe is check-then-act with no lock (concurrent duplicates can double-embed — wasted credits, not corruption); 1 scanned ACE PDF uningested (rehab appendix, needs OCR); ACE repo drift: `venv/bin/python scripts/ace_guidelines.py --list` (free), `--ingest` idempotent; k=12 retrieval heuristic — revisit as the corpus grows; WIDB parse is layout-fragile by design (3-day cache may serve a stale parse — `python -m context --no-cache` force-refetches); catchment geocoding for per-clinic URA planning signals (OneMap key or street→region table).

## Next-session prompt
Project: CP_RAG at `/Users/ugeneo/Documents/Project Codes/CP_RAG` — Singapore GP clinic's AI research assistant: FastAPI + LangChain RAG Q&A over primary-care chronic-care protocols **plus 96 ACG clinical-guideline PDFs (2,975 chunks; `source_site` routes them to the *Clinical guidelines* prompt section — never cited as protocol content)** + MOH public guidance + live local-context signals (NEA air, DENGBURDEN dengue clusters, MOM heat, CDA `disease_week` bulletin, URA planning `catchment_change` — all key-free via the `context/` package) **plus the Phase 6 clinician brief dashboard (hash-routed Brief (default) / Chat / Library tabs; on-demand one-page brief, 15-min cache; the only LLM path outside `/api/chat` is `POST /api/brief/generate`; corpus inventory via `GET /api/library`)**. LLM `deepseek/deepseek-v4-flash-0731` + `text-embedding-ada-002` via OpenRouter (env-configured in `.env`, git-ignored — never commit it). Golden rules in `CLAUDE.md`: credits are a budget (prefer `/api/status` over chat probes; a Generate click on a stale brief costs 1 chat-model call), Python 3.9, `chroma_db/` is expensive — don't rebuild. Read `CLAUDE.md` first; build notes + phase table: `docs/signal-research.md` §9/§12–16.

State: Phases 0–6 all done and **pushed to origin** (HEAD `21df710` = origin/main; SDD ledgers in `.superpowers/sdd/`, untracked local-only, Phase 6 ledger `2026-09-09-clinician-brief-library/ledger.md`). Server running detached on port 5001 (log `/tmp/cp_rag_server.log`; restart: `nohup venv/bin/python -m app > /tmp/cp_rag_server.log 2>&1 &`, cold start ~6 min, mostly embedding). Test clinic: **Lakeside Family Medicine Clinic**, 518A Jurong West Street 52 (lat 1.3454017, lon 103.7188383, PC 641518 — real GP clinic, non-polyclinic per user; richest live brief: west haze + 60-case dengue cluster 1.1 km). The test-clinic swap left `context/config.py`, `context/__init__.py`, `docs/signal-research.md`, `HANDOFF.md` **modified but UNCOMMITTED — commit only when the user says so**. No brief has been generated for the new clinic (brief cache cold after restart by design; Generate = 1 paid call, user's call). Corpus: 6,764 chunks (protocols + MOH + 96 ACE PDFs). No further phases remain in the §9 table — the backlog is the Outstanding list above, starting at item 1 (commit when the user is ready) or item 4 (upload progress indicator). Standing checks after any change: py_compile, cold start, /api/health, /api/status, /api/context (0 LLM credits), /api/brief (paid — do NOT generate unless asked).
