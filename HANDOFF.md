# HANDOFF — CP_RAG (updated 2026-09-09, Phase 6 — clinician brief dashboard + Library)

## Done this session (Phase 6, all gates green, committed)
The chat page is now three hash-routed tabs — Brief (default) / Chat / Library. The Brief tab is a clinician dashboard: 6 KPI tiles from the live snapshot + an on-demand one-page brief (headline / watch list / outlook) + a Protocol spotlight rendered 100% from structured `protocol_links.active`. The Library tab lists the corpus (totals, guideline-PDF grid, protocol pages, MOH guidance) and now hosts the PDF upload bar. **Phase credit total: 2 paid LLM calls** (budget was 1 — recorded deviation below); every other path verified at 0 credits.
- **`context/brief.py`** (new) — pure, stdlib-only snapshot→prompt projection; `protocol_links` structurally absent from the LLM payload (asserted in dry runs); strict JSON contract `{headline, watch[≤6], outlook}`.
- **`rag.py`** — `list_corpus()` (Task 1: PDFs + web sources by site + totals; legacy chunks without `source_site` fall back to URL host via `_site_of()`); `generate_brief()` (one chat-model call, no retrieval, no embeddings), `_parse_brief()` (fence-strip, strict JSON, type coercion, watch clip 9→6), `_apply_provenance_guard()` (drops watch items mentioning `protocol`/`guideline`/known protocol names; counted as `provenance_drops`).
- **`app.py`** — `GET /api/library` (503 on `rag_chain is None`, executor scan, 500 on scan failure); brief cache state (in-memory, keyed on 3-dp clinic coords, tied to the snapshot's `generated_at`; server restart = cold); `GET /api/brief` (free, never builds); `POST /api/brief/generate` — **the only LLM path outside `/api/chat`**, guard order per spec §7.2: invalid point 400 → KB-not-ready 503 → fresh cache (0 credits) → cached-error 502 (90-s failure sentinel) → in-flight shared future (stampede guard) → forced-regenerate 429 + `Retry-After` (60-s cooldown) → thread-pool build; 15-min success TTL.
- **`static/index.html`** — `<nav id="tabs">` + hash routing (`#/brief` default, `#/chat`, `#/library`; bogus hash → brief); 6 KPI tiles (Air/Weather/Dengue/WIDB/Planning/Nearest polyclinic, gap + degraded states per D10); brief renderer (headline / watch cards / outlook / provenance footer + parse-error fallback showing raw LLM text + "No brief yet" empty state); `generateBrief()` (the UI's only brief POST; double-click guard; staged progress; button flips to "Regenerate (costs 1 LLM call)" only when a fresh brief exists); `loadLibrary()`/`renderLibrary()` (totals line, 96-card PDF grid with source links + doc-hash prefixes, protocol section filtered to `/care-protocols/` with the >25 URL-sprawl collapse card + "49 other pages" footnote, MOH section with derived title + raw URL); `#pdf-bar` moved from Chat to Library; context strip survives verbatim in the Brief view, collapsed under "Raw context data" (DOM nodes never removed — the 15-min refresh intervals depend on them).
- **Scope rulings (spec, one-liners):** D1 full Library (PDFs + protocol pages + MOH + totals, not PDFs only); D2 Brief is the home view, the 7-card strip survives verbatim as a collapsed drill-down; D3 on-demand LLM (no retrieval/embeddings, 15-min cache per clinic point); D4 two-phase API (free GET + paid POST); D5 in-flight stampede guard (concurrent POSTs await one future → 0 extra LLM calls); D6 server-side 60-s force-regenerate cooldown → 429 + `Retry-After`; D7 90-s failure sentinel cached on LLM failure → 502, outage retries cost 0; D8 "1 Generate" = ≤2 provider round-trips (~2–3k tokens, 0 embeddings, 0 retrieval) — stated in UI microcopy + docs; D9 clinical-safety provenance (prompt strip + structured-only spotlight + server-side drop guard); D10 per-tile "unavailable (source)" gap states keyed off `data_gaps`; D11 task order Library backend → brief backend → UI → docs.
- **Verified (gates, all against the live server):** server boot + `/api/status` ready + `/` 200; `node --check` on the extracted page script; headless DOM harnesses with **zero runtime errors** — Brief harness (6 tiles, headline, 6 watch cards, outlook, spotlight, tab round trip incl. a manual `refreshContext()` tick from the Chat tab, bogus hash → brief) and Library harness (direct reload at `#/library`, 13/13: totals vs API, 96 PDF cards + collapse card, protocol "28 pages — collapsed" + "49 other pages" footnote, MOH row with raw URL + derived title, round trip). Library API 0 credits: totals **6,764 chunks / 96 guideline PDFs (2,975 chunks, matches Phase 5) / 28 protocol pages / 1 public-guidance page**. Free brief endpoints: stale/none + 400 bad point. **Paid record:** `POST /api/brief/generate` → 200, `deepseek/deepseek-v4-flash-0731`, valid JSON, headline + 5 sourced watch items, `provenance_drops: 0`, outlook names the PM2.5 gap (~91 s wall = cold snapshot + LLM); forced regenerate → **429 + `Retry-After: 59`** (0 credits); no-force after fresh → `cached: true` (0 credits, curl-verified).
- **Credit deviation (recorded in the ledger):** budget was 1 paid call; **2 were consumed.** The boot-time context warm-build completed mid-verification, rotating the 15-min snapshot cache; a no-force POST issued as a "cache probe" (status probes should use the FREE `GET /api/brief`) then saw the rotated snapshot → correct stale detection → paid rebuild. Cache/invalidation worked as designed; the probe was the deviation. No further paid calls this phase.
- **Commits (on `main`, ahead of `origin/main`):** `7454cce` spec + plan; `6403ff3` Task 1 (`/api/library`); `831a364` Task 2 (brief backend); `af1e31d` Task 3 (tabs + Brief UI); `22ed7ec` Task 4 (Library UI); + this docs commit.

- **Post-completion debt fixes (2026-09-09, 0 credits):** (1) `app.py` `/api/ingest-pdf` now streams the upload in 1 MB chunks and refuses at the 25 MB cap — a 500 MB test upload returned 400 with the server's RSS up only ~27 MB (previously the whole body was buffered before the cap check); 26 MB → 400, non-PDF → 400 (regression), no embedding activity in the log. (2) `rag.py`: the O(n) full-metadata scan is now cached in `_scan_metadatas()` (invalidated on `vectorstore._collection.count()` — O(1) in chromadb; note the langchain 0.2.x `Chroma` wrapper has no `count()` of its own), shared by `list_pdfs()` / `list_corpus()` / `_store_url_index()` / `_store_pdf_index()` — direct measure: 173.5 ms scan vs 12.5 ms cache hit, `list_corpus` ~19 ms; HTTP `/api/library` cold 183 ms / warm 21 ms (was ~100–280 ms per call); data-equality asserted across cache/invalidation states; both headless harnesses re-run, zero runtime errors.
## Outstanding (priority order)
1. Live PDF upload was NOT exercised this phase (embedding spend) — `POST /api/ingest-pdf` is unchanged and was verified in Phase 5; the Library tab now hosts the upload bar and refreshes the inventory after a successful upload.
2. Brief + snapshot caches are in-memory — lost on server restart (by design). A Generate click on a stale/cold brief is a paid build; `GET /api/brief` is the free status probe.
3. primarycarepages refresh needs a full `ingest()` rebuild (URL rewrite defeats append skip); postcode geocoding is district-centroid approx (OneMap DNS-blocked) — `--lat/--lon` is exact.
4. Carried from the Phase 5 post-completion review (pre-existing patterns, none security-relevant):
   - `rag.py` `ingest_pdf()` / `ingest_append()`: hash/URL dedupe is check-then-act with no lock — concurrent duplicate uploads can double-embed (wasted embedding credits, not corruption).
   - (the other two Phase-5 items were fixed 2026-09-09 — streaming-enforced upload cap + cached inventory scan; see the "Done this session" debt-fix line)
5. 1 scanned ACE PDF uningested (rehab appendix) — OCR would be needed; revisit only if that appendix matters.
6. ACE repo drift: `venv/bin/python scripts/ace_guidelines.py --list` (free) shows it; `--ingest` re-runs are idempotent (sha256 dedupe).
7. k=12 retrieval heuristic — revisit if answers over- or under-retrieve as the corpus grows.
8. WIDB is layout-fragile by design (regex over PDF text); `parse_widb()` fails loud on template changes, the 3-day cache can serve a stale parse meanwhile — `python -m context --no-cache` force-refetches.
9. Catchment geocoding for per-clinic URA planning signals: OneMap Geocoding API (free but needs its own key) or a street→region table.

## Next-session prompt
Project: CP_RAG at `/Users/ugeneo/Documents/Project Codes/CP_RAG` — FastAPI + LangChain RAG Q&A over Singapore primary-care chronic-care protocols **plus the full ACE clinical-guidelines repository (96 ACG PDFs, 2,975 chunks; `source_site` routes them to the *Clinical guidelines* prompt section — never cited as protocol content)** + MOH public guidance + live local-context signals (CDA bulletin `disease_week` Phase 3, URA planning `catchment_change` Phase 4) **+ the Phase 6 clinician brief dashboard (on-demand one-page brief, 15-min cache, the only LLM path outside `/api/chat` is `POST /api/brief/generate`) and the Library tab (corpus inventory via `GET /api/library`)**. LLM `deepseek/deepseek-v4-flash-0731` + `text-embedding-ada-002` via OpenRouter (env-configured in `.env`, git-ignored — never commit it). Golden rules in `CLAUDE.md`: credits are a budget (prefer `/api/status` over chat probes; a Generate click on a stale brief costs 1 chat-model call), Python 3.9, `chroma_db/` is expensive — don't rebuild. Build notes + phase table: `docs/signal-research.md` §9/§12–16. Read `CLAUDE.md` first.

State: **Phases 0–6 ALL DONE** (Phase 6 completed 2026-09-09: brief dashboard + Library, 2 paid calls total vs 1 budgeted — recorded deviation, §16), plus the 2026-09-09 post-completion debt fixes (streaming upload cap, cached inventory scan) — all committed and **pushed to origin** on 2026-09-09 (Phase-6 commits `7454cce`, `6403ff3`, `831a364`, `af1e31d`, `22ed7ec`, `a4841dd` + docs/debt-fix commits on top; see `git log`). SDD ledgers in `.superpowers/sdd/` (untracked, local-only; Phase 6 ledger: `.superpowers/sdd/2026-09-09-clinician-brief-library/ledger.md`). Server running detached on port 5001 (log: `/tmp/cp_rag_server.log`; restarted this session for the Phase 6 standing checks). Test clinic: Woodlands Polycline (lat 1.4309, lon 103.7752, PC 738579). Corpus: 6,764 chunks — protocols + MOH + 96 ACE guideline PDFs (2,975 chunks). No further phases remain in the §9 table — remaining work is the Outstanding list above.
