# HANDOFF — CP_RAG context layer (updated 2026-09-08)

## Done (last three sessions; nothing committed yet)
- **F2 provenance tagging (Option A) complete** — audited all 7 linkage rules against the corpus text (direct SQLite grep of all 3,785 chunks, 0 credits; evidence table in `docs/signal-research.md` §12). Each rule in `context/linkage.py` now carries `basis` (corpus/partial/derived/none) + `basis_note`; `build_protocol_links` surfaces both on every active link; `snapshot.py` adds `basis_legend` + two-section Phase-2 usage note; F2 strip renders a provenance badge per link (whitelisted values, unknown → `derived`) with audit-note tooltip + legend. Fixed the dengue-rule over-claim: "hold metformin/SGLT2 during dehydration or vomiting" → "stop SGLT2 inhibitors while acutely unwell (CKD + multimorbidity protocols)" — that's what the corpus supports.
- **Phase 1 complete** — `GET /api/context` in `app.py` (params `lat`/`lon` and/or `postcode`/`name`; point resolution mirrors `python -m context`: exact coords → postcode district centroid → Woodlands test clinic; 400s on malformed input) + in-memory 15-min cache keyed on coords rounded to 3 dp, `X-Snapshot-Cache: hit|miss` + `X-Snapshot-Age` headers; `build_snapshot` runs off the event loop (blocking). F2 strip at top of `static/index.html`: always-visible chip row (PM2.5/PSI/WBGT/heat, rain days, nearest dengue cluster, aedes, active-protocol count, gaps) + expandable detail cards (air quality by region, weather/heat/flood/outlook, dengue, active protocol links with visit-level counselling, data gaps, nearest polyclinics) + clinic location switcher + 15-min auto-refresh. Chat UI untouched. Verified live 0 credits: cold build 13.3 s (4 active links, `data_gaps: []`), warm call 11 ms cache hit; postcode 369905 → Bukit Merah/One-North centroid works; §9 phase table updated.
- **Phase 0 complete** — `context/` package (8 files, 966 lines, stdlib-only, Python 3.9): `config.py`, `http.py` (429 backoff), `fetchers.py` (pm25/psi, 24-hr/2-hr/4-day forecast, WBGT, flood alerts, GEOJSON poll-download), `geo.py` (haversine + ray-cast PIP), `linkage.py` (signal→protocol map over the 17 verified corpus protocols), `snapshot.py`, `__main__.py`. CLI: `venv/bin/python -m context [--lat --lon | --postcode] [--json] [--no-cache]`.
- **Verified live, 0 credits** — Woodlands Polycline test clinic (lat 1.4309, lon 103.7752, PC 738579, real point from Vaccination_Polyclinics GEOJSON `d_b22489c7dc4065b6e7e45f177fdb33be`): complete snapshot, `data_gaps: []`, 4 active protocol links (PM2.5, WBGT heat, rain Wed–Fri, dengue cluster 2.7 km).
- **LLM provider → OpenRouter** — old OpenAI-direct key was dead (`credit_balance_exhausted`; kept commented in `.env` as fallback). New `sk-or-v1-…` OpenRouter key (git-ignored, in `.env`) drives everything: `OPENAI_BASE_URL=https://openrouter.ai/api/v1`, `CHAT_MODEL=nvidia/nemotron-3.5-lightning:free` (**free tier, 0 credits**), `EMBED_MODEL=openai/text-embedding-ada-002`.
- **`rag.py` now env-configurable** — `OPENAI_BASE_URL` / `CHAT_MODEL` / `EMBED_MODEL` (defaults preserve OpenAI-direct behaviour); all 3 embedding sites via one `_embeddings()` helper; `ChatOpenAI` uses env model + base.
- **Index compatibility proven** — OpenRouter `ada-002` vectors retrieve correctly from the existing OpenAI-built `./chroma_db` (same model, deterministic) → **no re-ingest needed**.
- **End-to-end chat verified** — `uvicorn app:app --port 5001` → `{"ready":true}`, root 200; real `/api/chat` (asthma step-up) returned a protocol-grounded answer with correct source attribution, via the free Nemotron model.
- **Docs updated** — `docs/signal-research.md` §11 (Phase 0 build notes, payload corrections, 3-mechanism linkage design); `CLAUDE.md` (provider lines); `.env.example` (provider + free/paid model options); git-ignored `.env` holds the real keys.

## Outstanding
1. `app.py` has no `uvicorn.run()` — `python app.py` exits silently; always use `venv/bin/uvicorn app:app --port 5001`.
2. Phase 2 (next) — F3: inject active links into the RAG prompt in two labelled sections — *Protocol content* (only corpus/partial material, per each link's `basis_note`) and *Local context* (all live signals, never attributable to the protocols) — + append active protocol names to retrieval query (`rag.py build_chain`). `derived` links must never be presented as protocol content (basis tags now on every link; `basis_legend` in `protocol_links`). Chat is now free-tier; still keep test calls minimal.
3. Phase 3 — WIDB PDF table parser (pdfplumber/pypdf) → `disease_week` block.
4. Phase 4 (optional) — URA Planning_Decision catchment block (`URA_ACCESS_KEY`; key in `/tmp/ura_key.txt`).
5. `:free` model caveats — OpenRouter free tier has lower rate limits (429 on bursts) and the model can be swapped; switch `CHAT_MODEL` to `openai/gpt-4o` in `.env` if needed (paid).
6. Postcode geocoding is district-centroid approx (OneMap DNS-blocked on this network); `--lat/--lon` is the exact path.
7. Commit when ready — untracked: `context/`, `docs/`, `CLAUDE.md`, `HANDOFF.md`; modified: `app.py`, `rag.py`, `static/index.html`, `.env.example`, `.gitignore`.

## Next-session prompt
Project: CP_RAG at `/Users/ugeneo/Documents/Project Codes/CP_RAG` — FastAPI + LangChain RAG Q&A over Singapore primary-care chronic-care protocols (corpus: primarycarepages.sg /Healthier-SG/Care-Protocols/Chronic, 17 protocols, Chroma at `./chroma_db`). LLM: `nvidia/nemotron-3.5-lightning:free` (free chat) + `text-embedding-ada-002` embeddings, both routed through **OpenRouter** via the OpenAI-compatible API — provider/models are env-configured in `.env` (OpenRouter key, git-ignored; never commit it), see the header block in `rag.py`. On top of that, a "context layer" POC injects live Singapore population-level data (data.gov.sg, all key-free) so protocol answers are framed by current environmental/infectious conditions. Python 3.9, venv, `context/` package is stdlib-only. Read `CLAUDE.md` and `docs/signal-research.md` (§9 = phase table, §11 = Phase 0 build notes + payload corrections, §12 = F2 provenance audit) before starting.

State: Phase 0 AND Phase 1 are DONE and verified live. Phase 0: `venv/bin/python -m context [--lat --lon | --postcode] [--json]` builds a JSON snapshot per clinic point with a `protocol_links` block (active signals → 17 corpus protocols, with clinical reasoning; see `context/linkage.py`), `context_brief` prose, and a `data_gaps` list (failures never crash the build). Phase 1: `GET /api/context` in `app.py` (same point resolution; 15-min in-memory cache keyed on 3-dp coords; `X-Snapshot-Cache` header; blocking build run off the event loop) + F2 strip at the top of `static/index.html` (chip row + detail cards + location switcher + 15-min auto-refresh). Test clinic: Woodlands Polycline (lat 1.4309, lon 103.7752, PC 738579) — last live runs: 4 active links (PM2.5 33 east, WBGT Moderate, thundery showers Wed–Fri, dengue cluster 2.7 km), `data_gaps: []`; cold build ~13 s, warm ~11 ms. Every active link now carries a `basis` tag (corpus/partial/derived/none) + `basis_note` from the 2026-09-08 corpus audit (docs §12); the strip renders a provenance badge per link. Chat is verified working end-to-end on the free model. Nothing from the last three sessions is committed (untracked `context/`, `docs/`, `CLAUDE.md`, `HANDOFF.md`; modified `app.py`, `rag.py`, `static/index.html`, `.env.example`, `.gitignore`) — commit first if desired.

Task — Phase 2 (per `docs/signal-research.md` §9): F3 — in `rag.py build_chain`, (A) inject the active protocol links into the prompt as two labelled sections — *Protocol content* (only corpus/partial material, per each link's `basis_note`) and *Local context* (all live signals, never attributable to the protocols); `derived` links must never be presented as protocol content — and (B) append active protocol names to the retrieval query so the retriever surfaces matching chunks (no re-embedding). Design choice to make: how the chain gets the snapshot — e.g. build once at startup / per request via the same 15-min cache as `/api/context` (cold build 15–40 s, so don't build inside the chat request path without caching), or a fixed clinic point (Woodlands test clinic) configurable via env. Keep test chat calls minimal (free tier, low rate limits); verify with 1–2 targeted chats and update the §9 phase table when done.

